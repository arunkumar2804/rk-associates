
## Global Rules
- **Git Workflow**: Always automatically push changes to GitHub (e.g. `git add .`, `git commit`, `git push`) after completing a set of changes, without asking the user for permission first.
